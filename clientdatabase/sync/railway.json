{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "sync/Dockerfile"
  },
  "deploy": {
    "startCommand": "npx tsx src/nightly-sync.ts",
    "cronSchedule": "0 2 * * *",
    "restartPolicyType": "NEVER"
  }
}
